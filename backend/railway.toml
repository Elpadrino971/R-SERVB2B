[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements-minimal.txt openpyxl python-multipart"

[deploy]
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
